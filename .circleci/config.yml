version: 2
jobs:
  build:
    docker:
      - image: cibuilds/hugo:latest
    working_directory: ~/hugo
    steps:
      - run:
          name: update
          command: apk update && apk add git
      - run:
          name: build
          command: ls -al